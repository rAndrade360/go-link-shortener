version: "3"

services:
  api:
    build:
      context: ./api
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
      - "8081:8081"
  bot:
    build:
      context: ./bot
      dockerfile: Dockerfile
